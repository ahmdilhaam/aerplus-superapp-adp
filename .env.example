VITE_API_BASE_URL=https://aerplus-superapp-api.qyubit.io
# Optional: only needed if the API returns relative image paths (imageUrl/avatarUrl).
# Absolute/data/blob URLs are used as-is regardless of this setting.
VITE_IMAGE_BASE_URL=
